import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Healing | Nathanel",
  description: "Experience transformative energy healing practices that integrate mind, body, and spirit for holistic wellness.",
};

export default function HealingLayout({ children }: { children: React.ReactNode }) {
  return children;
} 